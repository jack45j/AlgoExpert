def fourNumberSum(array, targetSum):
    hash = {} #[Int: [[Int]]]
    result = [] # [[Int]] 
    
    for idx in range(1, len(array)):
        for idxR in range(idx + 1, len(array)):
            pass

        for idxL in range(0, idx):
            pass


    pass
